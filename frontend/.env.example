# Copie este arquivo para .env.local e preencha os valores
# Em Docker Compose, use o host interno do serviço backend
# Gere AUTH_SECRET com: openssl rand -base64 32
AUTH_SECRET=
NEXT_PUBLIC_API_URL=http://backend:8000
